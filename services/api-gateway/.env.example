# API Gateway Configuration
PORT=3000

# JWT Secret - REQUIRED: Use a strong, randomly generated secret
# WARNING: DO NOT use the example value below in production!
# Generate a secure random secret using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# SECURITY: Never commit your actual JWT_SECRET to version control
JWT_SECRET=

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# Signaling server used for mobile pairing / WebRTC
SIGNALING_HTTP_URL=http://localhost:4000

# Database (future)
# DATABASE_URL=postgresql://user:password@localhost:5432/agiworkforce
